import 'package:flutter/material.dart';

class MoreInfoCard extends StatelessWidget {
  const MoreInfoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
